{
  "name": "h5games_poki2",
  "version": "1.0.0",
  "description": "A lightweight, Poki-style HTML5 game portal. No frameworks, no build step — pure HTML / CSS / JS.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "mkdir -p dist/css && cat css/style.css css/pages.css > dist/css/styles.concat.css && postcss dist/css/styles.concat.css -o dist/css/styles.css && rm dist/css/styles.concat.css",
    "build:js": "rollup -c",
    "generate:og": "node scripts/generate-og.js",
    "hash:assets": "node scripts/hash-assets.js",
    "build:copy": "mkdir -p dist && rsync -a --exclude=node_modules --exclude=.git --exclude=scripts --exclude=tools --exclude=env --exclude=.venv --exclude=.history --exclude=.wrangler --exclude=dist ./ dist/",
    "build": "npm run generate:og && npm run build:css && npm run build:js && npm run hash:assets && npm run build:copy",
    "publish:pages": "wrangler pages publish ./dist --project-name \"${CF_PAGES_PROJECT:-h5games-poki2}\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "glob": "^13.0.1",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.0.0",
    "rollup": "^2.79.2",
    "rollup-plugin-terser": "^7.0.2",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "cssnano": "^7.1.2",
    "sharp": "^0.34.5",
    "node-fetch": "^3.3.1"
  }
}
